interface:
  display_name: "Codex Session Restore"
  short_description: "Salvage transcripts without merging old task state"
  default_prompt: "Use $codex-session-restore to preview transcript salvage from my trusted backups and verify CLI and Desktop separately."
